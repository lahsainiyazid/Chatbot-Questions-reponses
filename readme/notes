day 1: Normalized names of subdirectories in Questions a traiter for better metadata it has 196 pdfs so it is already good data and created chunks 
day 2: should restart chunking and change \n and weird controll characters such as :\u001a, and \u001b use them by replacing these ranges in regfex:
Range 1: \x00-\x1f (Standard ASCII Control Characters) Range 2: \x7f-\x9f (Delete and Extended Control Characters)
leanred about context-relevance ,faithfulness,answer relevance i find it hard to create test dataset so i will move on to query rewriting
day 3: Worked on working full data and found out that there is question->Answer dataset that i can build dataset i am also trying to use multilingual e-5 small to 
make model faster i need to fight time on a cpu with no api key or servers to host compute i need response time of 1-3 s while also good results and no hallucination
also got idea to export many api keys in crontab and rotate them i might need to free up enough latency by using smaller embedding models to learn more about rag 
Retrieval time: 0.3541 seconds
Reranker time:0.3541seconds   i did simpler model and got better faster results i can now add query rewriting using local ollama model added python script to fix file
corruption now i can do chunking and create tesset
